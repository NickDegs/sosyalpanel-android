<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 11+ paket görünürlüğü — sosyal uygulamaları başlatabilmek için (iOS LSApplicationQueriesSchemes karşılığı) -->
    <queries>
        <package android:name="com.instagram.android" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.twitter.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.instagram.barcelona" />
        <package android:name="com.pinterest" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.reddit.frontpage" />
        <package android:name="com.tumblr" />
        <package android:name="com.vkontakte.android" />
        <package android:name="xyz.blueskyweb.app" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.discord" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".SosyalPanelApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SosyalPanel">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode|locale|layoutDirection"
            android:theme="@style/Theme.SosyalPanel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".data.ReminderReceiver" android:exported="false" />
    </application>
</manifest>
